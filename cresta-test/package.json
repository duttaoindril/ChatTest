{
    "name": "cresta-test",
    "version": "0.1.0",
    "private": true,
    "dependencies": {
        "react": "^16.4.0",
        "react-dom": "^16.4.0",
        "react-hotkeys": "^1.1.4",
        "react-markdown": "^3.3.2",
        "react-scripts": "1.1.4",
        "react-toastify": "^4.1.0",
        "socket.io": "^2.1.1",
        "socket.io-client": "^2.1.1"
    },
    "scripts": {
        "start": "cd src/server && start nodemon index.js && cd .. && cd .. && react-scripts start",
        "serve": "cd src/server && node index.js",
        "reserve": "rm -rf build && npm run build && cd src/server && nodemon index.js",
        "build": "react-scripts build",
        "test": "react-scripts test --env=jsdom",
        "eject": "react-scripts eject"
    }
}
